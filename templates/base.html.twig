<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Bienvenue!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/fav-icon.png') }}">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
    <nav>
        <div class="nav-logo">
            <a href="{{ path('app_home') }}">
                <img src="{{ asset('img/menu-logo.png') }}" alt="GreenGoodies Logo">
            </a>
        </div>
        <div class="nav-links">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a href="#">Nos Produits</a>
                <a href="#">Mon Panier</a>
                <a href="#">Mon Compte</a>
                <a href="{{ path('app_logout') }}">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}">Connexion</a>
                <a href="{{ path('app_sign_up') }}">Inscription</a>
            {% endif %}
        </div>
    </nav>

        {% block body %}{% endblock %}
    </body>
    <footer><img src={{ asset('img/brandmark.png') }}></footer>
</html>
